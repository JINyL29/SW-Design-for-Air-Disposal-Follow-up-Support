# 📅 2026-06-11 작업 내용

---

## 1. GitHub에서 팀원 수정본 병합

팀원(Sonyoungjin)이 올린 `(수정본)` 파일들을 정리했습니다.

- 기존 파일에 `_1` 접미사를 붙여 백업
- `(수정본)` 파일들을 원본 이름으로 교체

**교체된 파일:**
- `main.py`
- `csc_06_dashboard_ui/dashboard_window.py`
- `csc_06_dashboard_ui/dashboard_log_widget.py`
- `csc_05_maintenance_support/csu_02_decision_message_builder.py`
- `csc_07_operator_control_ui/operator_window.py`

---

## 2. 부품 상태 테이블 흰 글씨 버그 수정

**원인:** `QTableWidgetItem` 생성 시 텍스트 색상을 명시하지 않아 시스템 테마에 따라 흰색으로 렌더링됨

**수정:** `dashboard_table_widget.py`의 `_item()` 함수에 `setForeground(QColor("#1A2B3C"))` 추가

---

## 3. BEC 고장 코드 "?" 버그 수정

**원인:** `BEC_OV_001`(과전압) 항목이 두 곳 모두 누락되어 있었음
- `csu_01_fault_code_mapper.py` — `("BEC", "OVER_VOLTAGE")` 매핑 없음
- `fault_code_db.json` — `BEC_OV_001` 항목 없음

**수정:** 두 파일 모두에 `BEC_OV_001` 추가 (심각도: HIGH)

---

## 4. 전체 부품 진단 버튼 버그 수정

**원인:** `_on_run_all()`이 모든 부품에 대해 항상 `(정상 최솟값 + 정상 최댓값) / 2`를 사용
→ 항상 NORMAL로 결과가 나와 이전에 주입한 고장이 덮어씌워짐

**수정:** `operator_window.py`에 `_last_diag` 딕셔너리를 추가해 각 부품의 마지막 진단 전압/시나리오를 기억하도록 변경

| 상황 | 동작 |
|---|---|
| 이전에 진단한 부품 | 마지막 전압/시나리오 재사용 |
| 한 번도 진단 안 한 부품 | 정상 mid 전압으로 진단 |
| 초기화 버튼 클릭 | 저장 이력 전체 삭제 |

---

## 수정된 파일 목록
```
csc_06_dashboard_ui/dashboard_table_widget.py
csc_04_fault_code_generator/csu_01_fault_code_mapper.py
data/fault_code_db.json
csc_07_operator_control_ui/operator_window.py
```
